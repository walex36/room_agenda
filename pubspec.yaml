name: room_agenda
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_neat_and_clean_calendar:
    path: 'packages/flutter_neat_and_clean_calendar'

  cupertino_icons: ^1.0.2
  provider: ^6.0.5
  flutter_modular: ^5.0.3
  cloud_firestore: ^4.7.1
  firebase_core: ^2.13.0
  intl: ^0.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true
